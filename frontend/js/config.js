// Same-origin by default because server.js serves this frontend directly.
// If you run the frontend separately (e.g. VS Code Live Server on a
// different port), change this to "http://localhost:5000/api".
const API_BASE = "/api";
